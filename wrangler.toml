name = "alphaarena-live"
compatibility_date = "2024-10-27"
pages_build_output_dir = ".vercel/output/static"

[observability]
enabled = true

# Cron Triggers for scheduled tasks
# Runs every 5 minutes to sync data from nof1.ai API
[triggers]
crons = ["*/5 * * * *"]
